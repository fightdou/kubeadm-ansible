#!/bin/bash
while true
do
    mount_state=`ls -l {{ desktop_mount_dir }} | wc -l`
    if [ $mount_state -le 5 ]
    then
        umount {{ desktop_mount_dir }}
        mount -a
        break
    fi
done
